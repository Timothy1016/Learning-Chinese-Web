export default function Loading() {
  return <main className="app-loading" aria-busy="true" aria-live="polite">
    <span className="brand-mark">龙</span>
    <p>Preparing your Chinese world…</p>
  </main>;
}
